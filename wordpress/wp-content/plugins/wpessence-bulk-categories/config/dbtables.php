<?php
// Unset the DB tables variable if it is set
unset($dbtables);

// Set the scope for the variable to store the data in to global
global $dbtables;

// Plugin data
$dbtables = array();
?>